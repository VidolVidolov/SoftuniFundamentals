function repeatingStrings(text, repitor) {

    // let result = '';
    // for (let i = 0; i < repitor; i++) {

    //     result += text;
    // }

    // return result;

    let result = text.repeat(repitor);
    console.log(result);
    

}

repeatingStrings(`abc`, 3
);